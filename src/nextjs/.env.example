# NextJS 用の環境変数。`cp .env.example .env` して埋める。

# Prisma / アプリが接続する PostgreSQL。
DATABASE_URL=postgres://user:password@host:5432/segnote-dev

# アップロードした録音の保存先ディレクトリ(絶対パス)。
# worker(src/worker)と同じ場所を指すこと。dev では通常 <repo>/src/storage。
SEGNOTE_STORAGE_DIR=/abs/path/to/segnote/src/storage
