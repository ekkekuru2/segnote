[project]
name = "segnote-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "inaspeechsegmenter>=0.8.0",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
]
